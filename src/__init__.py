"""Application source package: components, data, utils.

Streamlit page files import from this package using absolute paths
(e.g. ``from src.utils.palette import OKABE_ITO``) so the codebase
stays navigable regardless of which page is the current entry point.
"""
