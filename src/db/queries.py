"""Reusable SQL for the dashboard.

Placeholder. Every public function should return a tidy
``pandas.DataFrame`` and be wrapped in ``@st.cache_data`` at the
call-site (or inside the function itself, with a ``ttl`` that matches
how fresh the data needs to be).

Schema reference: see ``docs/data_schema.md``.
"""

from __future__ import annotations
